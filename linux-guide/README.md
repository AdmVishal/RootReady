# root_n_reels Detailed Linux Administration Guide

This static site contains 28 detailed chapters aligned to the supplied Linux administration syllabus, rewritten as original educational content.

## GitHub Pages
Upload `index.html` and `styles.css` to a repository, then enable Settings → Pages → Deploy from branch → main → root.

## Notes
Validate all administrative commands in a lab before production use. Distribution versions and defaults differ.
